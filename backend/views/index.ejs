<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <% if (user) { %>
        <p>You are logged in as <%= user.email %>.</p>
        <p>Your role is <%= user.role %>.</p>
        <a href="/auth/logout">Logout</a>
    <% } else { %>
        <p>You are not logged in. <a href="/auth/login">Login here</a></p>
    <% } %>
</body>
</html>
